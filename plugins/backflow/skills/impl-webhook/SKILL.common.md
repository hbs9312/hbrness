---
name: impl-webhook
description: TS §10 → inbound webhook controller (서명 검증 + idempotency + enqueue) + sender adapter + facade. "webhook 구현", "결제 알림", "이벤트 수신" 요청 시 사용.
argument-hint: [기술 명세서 경로]
tools: [file:read, search:grep, search:glob, file:write, file:edit]
effort: max
---

# Inbound Webhook 구현 (B-WEBHOOK)

ultrathink

당신은 백엔드 통합 엔지니어입니다.

TS §10 외부 연동·Webhook → inbound webhook controller (서명 검증 + idempotency + enqueue) + sender 어댑터 + facade + idempotency entity를 구현합니다.

## 컨텍스트 로드

- **프로젝트 설정**: [backend.md](../../context/backend.md) — `webhook.*` 섹션 **필수**, `structure.*`, `framework.*`, `database.orm`, `external_services.message_queue`
- **태스크-파일 맵**: `.backflow/task-file-map.md` (있으면)
- **서비스 레지스트리**: `.backflow/service-registry.md` (기존 큐 추상 검출용)

## 입력

$ARGUMENTS 에서 기술 명세서(TS) 파일 경로를 추출하여 Read 로 읽으세요.

필수 입력:
1. **TS §10 외부 연동·Webhook** — webhook_id / sender / endpoint / signature_alg / signature_header / signature_secret_env / event_types / idempotency_key_source / timeout_sec / always_200
2. **TS §3.2 OpenAPI fragment** — operationId 검증 (`receive{WebhookIdCamel}` 존재 확인)
3. **TS §4 에러 코드 맵** — WEBHOOK_SIGNATURE_INVALID / WEBHOOK_TIMESTAMP_REPLAY / WEBHOOK_IDEMPOTENCY_KEY_MISSING / WEBHOOK_REQUEST_HASH_MISMATCH 자동 추가 권고. **WEBHOOK_DUPLICATE_DELIVERY 는 에러가 아닌 idempotency hit** (XR-009)
4. **TS §5 데이터 모델** — idempotency entity 의 관계 도출
5. **TS §처리 흐름** — handler 큐 dispatch 패턴 확인

## ★ 실행 위치 — impl-file-upload 후, export-api-contract 전 ★

```
impl-file-upload          ← TS §9 파일 처리   ★Phase 1 (5)
     │
     ▼
impl-webhook              ← TS §10 외부 연동·Webhook (서명 + idempotency + enqueue)   ★Phase 1 (6)
     │
     ▼
export-api-contract       ← TS §3.2 + §4 + 라우트(file-upload + webhook controller 포함)   ★Phase 1 (3)
```

## 선행 skill 책임 경계

### `impl-controllers` 와의 경계

- TS §10 의 webhook operationId (`receive{WebhookId}`) 는 `impl-controllers` 가 stub/skip
- 본 skill 이 실제 controller 핸들러 작성 (NestJS 시 별도 WebhooksModule/controller 생성 권장)

### `impl-middleware` 와의 경계

- `impl-middleware` 는 `backend.md.webhook.bypass_auth_routes` 의 경로에 대해 auth guard bypass 만 보장
- 본 skill 이 signature 검증 middleware 를 작성하고 bypass 경로에 적용

### `impl-integrations` 와의 경계

- 메시지 큐 추상이 있으면 본 skill 의 dispatch 가 그 큐를 사용 (재구현 금지)
- `.backflow/service-registry.md` 에서 기존 큐 추상 검출 후 wrapping

## SignatureAdapter 인터페이스 (XR-001, XR-004)

`backend.md.webhook.webhook_module_dir` / `backend.md.webhook.signatures_subdir` / `types.ts` 에 생성:

```typescript
// webhook/signatures/types.ts
export interface SignatureVerifyInput {
  rawBody: Buffer | string;       // raw bytes (서명 일관성)
  headers: Record<string, string | string[]>;
  secret: string;
  clockSkewSec: number;
}

export interface SignatureVerifyResult {
  valid: boolean;
  reason?: string;                // production 응답 노출 금지
  timestamp?: number;             // replay 검사용
}

export interface SignatureAdapter {
  readonly sender: string;
  readonly alg: string;
  /**
   * MUST: HMAC 직접 비교 시 crypto.timingSafeEqual (Node) 또는 동등한
   * constant-time 비교 사용. SDK 검증 API 사용 시 SDK 가 보장 (Stripe 등).
   * MUST: timestamp 가 추출 가능한 alg 에서 timestamp 반환.
   */
  verify(input: SignatureVerifyInput): SignatureVerifyResult;
}
```

## selected-signature.ts facade (XR-001)

`backend.md.webhook.webhook_module_dir` / `backend.md.webhook.signatures_subdir` / `backend.md.webhook.selected_signature_filename` 에 생성:

```typescript
// AUTO-GENERATED by backflow:impl-webhook
// 정적 dispatch — TS §10 의 모든 sender 를 build-time 에 fix.
// 런타임 if/switch 또는 dynamic import 금지 (validate §11.2 critical).

import type { SignatureAdapter } from './types';
import { stripeSignatureAdapter } from './stripe';
import { githubSignatureAdapter } from './github';
// ... TS §10 의 모든 sender import (정적)

const ADAPTERS: Record<string, SignatureAdapter> = {
  stripe: stripeSignatureAdapter,
  github: githubSignatureAdapter,
  // ... TS §10 의 모든 sender entry
};

export function getAdapter(sender: string): SignatureAdapter {
  const a = ADAPTERS[sender];
  if (!a) throw new Error(`No SignatureAdapter for sender: ${sender}`);
  return a;
}
```

**규칙** (XR-001):
- controller / service / dispatch / signatures/types.ts 에서 sender SDK import 직접 금지
- selected-signature.ts 는 정적 Record dispatch 만. 런타임 if/switch / dynamic import 금지
- TS §10 의 모든 sender 가 ADAPTERS 에 등장해야 함

## timing-safe compare 의무 (XR-004)

- `signatures/{sender}.ts` 에서 HMAC 직접 비교 시 반드시 `crypto.timingSafeEqual` (Node) 또는 동등 constant-time 비교 사용
- `===`, `Buffer.compare`, `==` 으로 서명 비교 금지
- SDK 검증 API (`Stripe.webhooks.constructEvent` 등) 사용은 timing-safe 보장으로 허용

## sender 식별자 영역 격리

- `signatures/{sender}.ts` 내부에만 sender SDK import 허용
- controller / service / dispatch / signatures/types.ts / selected-signature.ts 에 sender 식별자(SDK import, 벤더 변수명) 0건
- `signatures/none.ts` 는 `NODE_ENV=development` 가드 **필수** (production 차단)

## raw body 보존 — framework 별 (XR-006)

| framework | 패턴 |
|---|---|
| **NestJS** | `NestFactory.create(AppModule, { rawBody: true })` + `@Req() req: RawBodyRequest<Request>` 에서 `req.rawBody` |
| **Express** | webhook route 만 `express.raw({ type: '*/*' })` (전역 `express.json()` 앞에 배치). 비-webhook route 는 일반 json parser |
| **Fastify** | `fastify.addContentTypeParser('*', { parseAs: 'buffer' }, ...)` route-specific. 또는 `@fastify/raw-body` plugin |
| **FastAPI** | `Request.body()` await — Pydantic 파싱 전 호출. 또는 dependency injection 으로 raw bytes 주입 |
| **Spring Boot** | `HttpServletRequest.getInputStream()` 또는 `@RequestBody byte[] body` (filter 가 wrap 하지 않은 환경) |

skill 은 framework 검출 후 적절한 패턴으로 boilerplate 작성. validate-code §11.1 `raw_body_preserved` 가 framework 별 anchor 검사.

## Controller 동작 — default flow (XR-002, XR-003)

```
1. raw body 캡처 (framework 별)
2. webhook_id 결정 (route 매칭)
3. webhookCfg = TS §10 lookup (webhook_id)
4. signatureAdapter = getAdapter(webhookCfg.sender)
5. result = signatureAdapter.verify({ rawBody, headers, secret, clockSkewSec })
   - invalid → 401 + WEBHOOK_SIGNATURE_INVALID (timing-safe 검증 실패)
6. timestamp replay 검사 (result.timestamp 있으면):
   - |now - timestamp| > clockSkewSec → 400 + WEBHOOK_TIMESTAMP_REPLAY
7. idempotency_key 추출 (TS §10.idempotency_key_source grammar 적용)
   - missing/empty → 400 + WEBHOOK_IDEMPOTENCY_KEY_MISSING
8. request_hash = sha256(rawBody)
9. INSERT ON CONFLICT (XR-002):
   INSERT INTO webhook_idempotency (id, webhook_id, idempotency_key, request_hash, status, expires_at)
   VALUES (..., 'pending', now() + interval '30 days')
   ON CONFLICT (webhook_id, idempotency_key) DO NOTHING
   RETURNING id;

   - 새 행 생성 (새 delivery): 정상 진행
   - 기존 행 존재 (idempotency hit):
     - 기존 행 status = complete: stored response_status / response_body 그대로 반환 + WEBHOOK_DUPLICATE_DELIVERY info log (XR-009 — 에러 아님)
     - 기존 행 status = pending/processing: 즉시 200 + "duplicate in flight" log (handler 재실행 안 함) — Phase 1 default
     - 기존 행 request_hash != now hash: 409 + WEBHOOK_REQUEST_HASH_MISMATCH (같은 key 다른 body — sender 버그 가능성)
10. queue enqueue (impl-integrations 의 큐 사용):
    - enqueue 실패 + always_200=true: 200 + idempotency 행 status: failed (worker 가 나중에 cleanup)
    - enqueue 실패 + always_200=false: 503 + idempotency 행 삭제 (sender 재시도 유도)
    - enqueue 성공: 200 즉시 응답
11. async worker (별도 process):
    - handler 실행 → idempotency 행 status: complete + response_body/status 기록
    - handler 실패 → status: failed + error 기록 + 재시도 정책 (impl-integrations 큐 의존)
```

**원칙** (XR-003): controller 는 enqueue 만. handler 결과는 worker 가 비동기로 idempotency 행 갱신. `always_200=false` 는 enqueue 실패 시에만 적용 — handler 실패 시점은 controller 가 모름.

## idempotency_key_source minimal grammar 파서

```
source := term ('+' term)*
term   := header(NAME)                      # 헤더 전체 값
        | headerParam(NAME, PARAM)          # 'Stripe-Signature: t=123,v1=abc' 의 t 값
        | body(PATH)                        # JSON dot path (dot notation)
        | fallback(term, term)              # 첫 번째 missing/empty 시 두 번째
```

- 모든 term 결과가 missing/empty → `WEBHOOK_IDEMPOTENCY_KEY_MISSING` 에러 (400)
- concat delimiter: `:` (구현 fix)

## always_200 per-webhook override (XR-008)

- TS §10.always_200 컬럼 명시 시 그 값 우선
- 미명시 시 `backend.md.webhook.always_200_default` 사용 (default: true)
- `always_200=false` 시 enqueue 실패 → `backend.md.webhook.retry_status_code` (default 503)

## WEBHOOK_TIMESTAMP_REPLAY vs WEBHOOK_DUPLICATE_DELIVERY 분리 (XR-009)

- `WEBHOOK_TIMESTAMP_REPLAY`: timestamp 검사 실패 (clock skew 초과). 에러 응답 (400)
- `WEBHOOK_DUPLICATE_DELIVERY`: idempotency hit (정상 중복). 에러가 아닌 info log. 200 응답

## Idempotency entity canonical schema

| 필드 | 타입 | 비고 |
|---|---|---|
| `id` | UUID v7 | PK |
| `webhook_id` | text | indexed |
| `idempotency_key` | text | |
| `request_hash` | text | sha256(rawBody). 같은 key 다른 body 검출 |
| `status` | enum (`pending` / `processing` / `complete` / `failed`) | |
| `response_body` | jsonb? | replay 시 재사용 |
| `response_status` | int? | |
| `error` | text? | failed 시 |
| `expires_at` | timestamptz | created_at + ttl_days |
| `created_at`, `updated_at`, `completed_at?` | timestamptz | |

**`UNIQUE(webhook_id, idempotency_key)` constraint** (race 방지 핵심).

## 출력 파일

경로는 `backend.md.webhook.*` 와 framework convention 에서 derive:

- `{webhook_module_dir}/webhooks.controller.ts` (또는 framework 패턴)
- `{webhook_module_dir}/webhooks.service.ts`
- `{webhook_module_dir}/webhooks.module.ts` (NestJS 시)
- `{webhook_module_dir}/dto/*.dto.ts`
- `{webhook_module_dir}/{signatures_subdir}/types.ts` — SignatureAdapter 인터페이스
- `{webhook_module_dir}/{signatures_subdir}/none.ts` — 개발용 (NODE_ENV=development 가드)
- `{webhook_module_dir}/{signatures_subdir}/{sender}.ts` — TS §10 의 sender 마다
- `{webhook_module_dir}/{signatures_subdir}/{selected_signature_filename}` — 정적 facade
- `{webhook_module_dir}/{dispatch_file}` — 큐 enqueue 추상
- `{idempotency_entity_path}` — idempotency entity
- 마이그레이션 파일 (ORM 별)

## Framework 별 출력 분기

`backend.md.framework.name` + `backend.md.framework.language` 기준:

| framework | controller | service | module/routing |
|---|---|---|---|
| **NestJS (TypeScript)** | webhooks.controller.ts (@Controller) | webhooks.service.ts | webhooks.module.ts (@Module) + AppModule import |
| **Express (TypeScript)** | webhooks/router.ts (Router) | webhooks/service.ts | app.ts use(router) |
| **Fastify (TypeScript)** | webhooks/route.ts (FastifyPluginCallback) | webhooks/service.ts | server.ts register(plugin) |
| **FastAPI (Python)** | webhooks/router.py (APIRouter) | webhooks/service.py | main.py include_router |
| **Spring Boot (Java/Kotlin)** | WebhooksController.java (@RestController) | WebhooksService.java (@Service) | @ComponentScan 자동 |

## 마이그레이션 시나리오

### Scenario A — 신규
TS §10 작성 → 전체 생성. idempotency 마이그레이션 실행.

### Scenario B — 기존 webhook 코드 산재
1. `--dry-run` grep: `crypto.createHmac`, `Stripe.webhooks.constructEvent`, `verifySignature`, `X-Hub-Signature`, `X-Slack-Signature`
2. webhook_id / signature_alg / source 추론
3. 사용자 승인 후 통합. timing-safe 비교 자동 codemod (`===` → `timingSafeEqual`)

### Scenario C — sender 추가
새 webhook_id + `signatures/{newSender}.ts`. `selected-signature.ts` 의 ADAPTERS 객체에 1행 append. 기존 controller/service 보존.

### Scenario D — idempotency 가 다른 형식으로 존재
1. `idempotency_entity_path` 를 기존 entity 로
2. canonical 충족 시 보존, 비호환 시 사용자 confirm

## 품질 자가 점검

- [ ] TS §10 의 모든 webhook_id 가 controller 핸들러/operationId 에 등장
- [ ] raw body 가 framework 별 anchor 패턴으로 캡처 (서명 검증 전)
- [ ] 서명 검증이 timing-safe compare 의무 (crypto.timingSafeEqual 또는 SDK API)
- [ ] signatures/none.ts 가 NODE_ENV=development 가드 보유
- [ ] selected-signature.ts 가 정적 Record dispatch 만 (SDK import / 런타임 분기 금지)
- [ ] controller/service/dispatch/types.ts/selected-signature.ts 에 sender SDK import 0건
- [ ] INSERT ON CONFLICT 패턴 (UNIQUE constraint 기반 race 방지)
- [ ] status: complete 인 idempotency hit 시 stored response 그대로 반환
- [ ] WEBHOOK_DUPLICATE_DELIVERY 가 에러가 아닌 info log (XR-009)
- [ ] enqueue-only default — controller 가 handler 직접 호출 X (XR-003)
- [ ] always_200 per-webhook override + backend.md.webhook.always_200_default fallback
- [ ] 서명 검증 실패 시 401/403 (200 금지)
- [ ] idempotency entity 에 UNIQUE(webhook_id, idempotency_key) constraint
